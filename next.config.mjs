/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
          'encrypted-tbn0.gstatic.com',
          'cdn.culturagenial.com',
          'upload.wikimedia.org',
          'www.spain.info'
        ],
      },
    };

export default nextConfig;
